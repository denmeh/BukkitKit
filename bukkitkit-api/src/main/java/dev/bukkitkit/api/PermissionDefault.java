package dev.bukkitkit.api;

/**
 * Default grant for a permission node — maps to {@code plugin.yml} {@code permissions.*.default}.
 */
public enum PermissionDefault {

    TRUE,
    FALSE,
    OP,
    NOT_OP
}
